---
'@config-store/react': minor
---

- Fix package.json exports.
- Update to support latest core.
- Reimplement `useConfig` and `useUpdateConfig`.
- Split `useUpdateConfig` into `useUpdateConfig` and `useUpdateConfigReducer`.
- Expose `ConfigSelector`, `ConfigMutator` and `ConfigReducer` types.
